#pragma once

#include "settings\settings_enum_type.h"

// T must be of settings_enum_type
template<typename T>
class settings_enum_combo_ui : public CComboBox
{
public:
    explicit settings_enum_combo_ui()
    {

    }
    
};